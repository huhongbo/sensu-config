{
  "client": {
    "name": "<%= node.hostname %>",
    "address": "<%= node.ipaddress %>",
    "subscriptions": [
	  "check_default",
	<% case node["platform"] %>
		<% when "ubuntu" %>
		"linux"
		<% when "aix" %>
		"aix"
		<% when "hpux" %>
		"hpux"
	<% end %>
    ]
  }
}
