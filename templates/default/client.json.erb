{
  "client": {
    "name": "<%= node.hostname %>",
    "address": "<%= node.ipaddress %>",
    "subscriptions": [
	  "check_default",
	  "del_log",
	<% case node["platform"] %>
		<% when "ubuntu" %>
		"linux"
		<% when "aix" %>
		"aix"
		<% when "hpux" %>
		"hpux"
	<% end %>
    ]
  }
}
