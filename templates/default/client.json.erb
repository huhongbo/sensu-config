{
  "client": {
    "name": "<%= node.hostname %>",
    "address": "<%= node.ipaddress %>",
    "subscriptions": [
	  "system",
	  "del_log"<% if node["platform"] == "hpux" %>,
	  "hpux"
	<% end %>
    ]
  }
}
