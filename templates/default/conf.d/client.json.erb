{
  "client": {
    "name": "<%= node.hostname %>",
    "address": "<%= node.ipaddress %>",
    "subscriptions": [
	  "system",
	<% if node["tags"].include?("cpu") %>
	  "system_other",
	<% end %>
	  "del_log"<% if node["platform"] == "hpux" %>,
	  "hpux"
	<% end %>
  	]
  }
}
